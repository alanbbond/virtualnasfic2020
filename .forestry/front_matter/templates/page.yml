---
label: Page
hide_body: false
fields:
- type: text
  name: title
  label: Title
  description: This is what shows up in the nav menu, and the end of the URL. So keep
    it short. If you want a longer title, just put it in a large font at the top of
    the page content.
- type: text
  name: layout
  label: Layout
  description: Everything should just use "default" for this except for the Chat page.
    That special layout allows our Discord to take up the full page width.
- type: text
  name: category
  label: Category
  description: We could make new categories like "public" vs "access_controlled" if
    we want. For now, this page won't be shown in the menu unless this is set to "page".
  hidden: false
- type: number
  name: order
  label: Order In Menu
  description: The order in which this page appears in the navigation menu.
