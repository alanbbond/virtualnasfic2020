---
label: Event In Schedule
hide_body: true
fields:
- name: schedule_entries
  type: field_group_list
  fields:
  - name: title
    type: text
    config:
      required: false
    label: title
    description: The title of the event. If it is more than 80 characters, it is recommended
      to clip something off and put it at the start of the description.
  - name: datetime
    type: text
    config:
      required: false
    label: datetime
    description: The day and time.
  - name: room
    type: select
    default: []
    config:
      required: false
      options:
      - Auditorium
      - Scioto Room
      - Olentangy Room
      source:
        type: simple
        section: 
        file: 
        path: 
    label: room
  - name: programparticipants
    type: list
    config:
      use_select: true
      min: 
      max: 
      source:
        type: pages
        section: program-participant-entries
    label: programparticipants
  - name: moderators
    type: list
    config:
      use_select: false
      min: 
      max: 
    label: moderators
  - name: description
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: true
      schema:
        format: markdown
    label: description
  config:
    min: 
    max: 
    labelField: 
  label: Schedule Entries
pages:
- _data/schedule.yml
