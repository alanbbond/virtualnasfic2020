---
label: Dealer
hide_body: true
fields:
- type: field_group_list
  name: Dealers
  label: Dealers
  fields:
  - type: text
    name: name
    label: name
    description: The name of this dealer or their business.
  - name: contact
    type: text
    config:
      required: false
    label: contact
  - name: city
    type: text
    config:
      required: false
    label: city
  - name: state
    type: text
    config:
      required: false
    label: state
  - name: what_is_sold
    type: text
    config:
      required: false
    label: What is sold
  - type: textarea
    name: description
    label: description
    description: The descriptive paragraph about this dealer.
  - type: text
    name: link
    label: link
    description: 'A link to this dealer''s external site. You may type Markdown links
      with braces and parens, like this: <br>```[link the reader will see](<a href="https://full-url.com/with-all-segments/"
      title="https://full-url.com/with-all-segments/" target="_blank">https://full-url.com/with-all-segments/</a>)```'
  - name: restrictions
    type: text
    config:
      required: false
    label: restrictions
  - name: image_filenames
    type: image_gallery
    config: {}
    label: image filenames
    description: A list of images that appear in this dealer's gallery. Click here
      to select or replace images, which you have uploaded in the Media link on the
      sidebar.
pages:
- _data/dealers.yml
